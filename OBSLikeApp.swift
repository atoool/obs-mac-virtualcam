import SwiftUI

@main
struct OBSLikeApp: App {
    var body: some Scene {
        WindowGroup("OBS-Like Studio") {
            ContentView()
        }
        .windowStyle(.titleBar)
        .windowToolbarStyle(.unifiedCompact)
    }
}
